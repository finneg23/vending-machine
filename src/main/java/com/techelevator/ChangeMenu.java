package com.techelevator;

import java.math.BigDecimal;
import java.math.RoundingMode;

public class ChangeMenu extends VendingMachineCLI{

//    private BigDecimal change;
//    private BigDecimal quarters;
//    private BigDecimal dimes;
//    private BigDecimal nickels;
//    public BigDecimal makeChange (BigDecimal currentMoneyProvided) {
//        quarters = ((BigDecimal.valueOf(2.65).divide(BigDecimal.valueOf(0.25))).setScale(0, RoundingMode.DOWN));
//        System.out.println(quarters);
//        dimes = ((BigDecimal.valueOf(2.65).subtract(quarters.multiply(BigDecimal.valueOf(0.25)))).divide(BigDecimal.valueOf(0.10))).setScale(0, RoundingMode.DOWN);
//        System.out.println(dimes);
//        nickels = ((BigDecimal.valueOf(2.65).subtract(quarters.multiply(BigDecimal.valueOf(0.25)))).subtract(dimes.multiply(BigDecimal.valueOf(0.10))).divide(BigDecimal.valueOf(0.05))).setScale(0, RoundingMode.DOWN);
//        System.out.println(nickels);
//        return change;
//    }
//TODO format this method to update balance to 0, pull from currentmoneyprovided, and print in a user friendly way
}

